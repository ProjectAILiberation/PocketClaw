# ============================================
# PocketClaw 聊天频道配置示例
# 将需要的变量复制到 .env 文件中即可启用
# 或运行 scripts/setup-channels.sh (macOS/Linux)
#       scripts\setup-channels.bat (Windows)
# ============================================
#
# ⚠️ 安全（必读）：每个公网可达频道都【必须】配套设置发送方白名单 <CHAN>_ALLOW_FROM，
#    否则互联网上任何陌生人都能私聊你的 AI 助手（它有完整工具权限、且用你自费的 API Key）。
#    未设置白名单的频道会在启动时被【自动跳过】，不会接入。白名单为逗号分隔的用户/号码 ID。

# ── Telegram（最简单，推荐首选）──
# 获取方法: Telegram 搜索 @BotFather → /newbot
# 白名单填你的 Telegram 数字 user id（可向 @userinfobot 询问）
# TELEGRAM_BOT_TOKEN=123456:ABCdefGhIjKlMnOpQrStUvWxYz
# TELEGRAM_ALLOW_FROM=123456789

# ── Discord ──
# 获取方法: https://discord.com/developers/applications
# 白名单填允许私聊的 Discord 用户 ID
# DISCORD_BOT_TOKEN=your-discord-bot-token
# DISCORD_ALLOW_FROM=123456789012345678

# ── Slack（需要两个 Token）──
# 获取方法: https://api.slack.com/apps
# SLACK_BOT_TOKEN=xoxb-your-bot-token
# SLACK_APP_TOKEN=xapp-your-app-token
# SLACK_ALLOW_FROM=U0123ABC

# ── WhatsApp ──
# 首次启动后查看日志扫码: docker compose logs -f pocketclaw
# WHATSAPP_ALLOW_FROM=+8613800138000,+8613900139000

# ── Signal ──（OpenClaw 2026.4+ 使用 account 字段，PocketClaw 内部已自动处理）
# 需要 signal-cli，详见: https://docs.openclaw.ai/channels/signal
# SIGNAL_PHONE_NUMBER=+8613800138000
# SIGNAL_ALLOW_FROM=+8613900139000

# ── Microsoft Teams ──
# 需要 Azure Bot Framework，详见: https://docs.openclaw.ai/channels/msteams
# MSTEAMS_APP_ID=your-app-id
# MSTEAMS_APP_PASSWORD=your-app-password
# MSTEAMS_ALLOW_FROM=29:1xxxx

# ── Google Chat ──（访问控制走 Google 侧的 space 成员，不使用 ALLOW_FROM）
# 需要 Google Cloud 服务账号，详见: https://docs.openclaw.ai/channels/googlechat
# 注意：当前 OpenClaw 版本已移除顶层 spaces 配置（GOOGLE_CHAT_SPACES 不再生效）
# GOOGLE_CHAT_CREDENTIALS=/path/to/service-account.json

# ── Matrix ──（访问控制走房间成员，不使用 ALLOW_FROM）
# 可使用 matrix.org 或自建服务器
# MATRIX_HOMESERVER=https://matrix.org
# MATRIX_USER_ID=@mybot:matrix.org
# MATRIX_ACCESS_TOKEN=your-access-token

# ── BlueBubbles (iMessage) / Zalo ──
# 注意：这两个频道的配置 schema 在当前 OpenClaw 版本已变更/移除，
#       本启动器暂不自动生成。如确需使用，请用 `openclaw configure` 手动配置，
#       并用 `openclaw config validate` 校验后再启动。
